% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reject_sampling.R
\name{reject_sampling}
\alias{reject_sampling}
\title{Sample angles from the star-shaped angular density via rejection sampling}
\usage{
reject_sampling(n, posterior_params, dim, gauge_type)
}
\arguments{
\item{n}{Integer. Number of angular samples to generate.}

\item{posterior_params}{Numeric vector of dependence parameter(s) for the
gauge function. For single-parameter gauges (all except Dirichlet),
a scalar or length-1 vector. For the Dirichlet gauge, a length-2 vector
c(theta1, theta2).}

\item{dim}{Integer. Dimension of the data (currently only dim = 2 supported).}

\item{gauge_type}{Character string specifying the gauge function family.
One of \code{"gauss"}, \code{"logistic"}, \code{"inv_log"},
\code{"asym_log"}, \code{"dirichlet"}, \code{"rectangular"}.}
}
\value{
Numeric vector of length \code{n} containing sampled angles
  in (0, 1).
}
\description{
Generates independent draws from the star-shaped angular density
f_W(w) proportional to g(w, 1-w; theta)^(-dim) using a uniform
envelope on (0, 1). Used for simulation and importance-sampling
prediction tasks.
}
\examples{
\dontrun{
# Sample 5000 angles from a Gaussian gauge star density with dep = 0.7
w_samples <- reject_sampling(n = 5000, posterior_params = 0.7,
                             dim = 2, gauge_type = "gauss")
hist(w_samples, breaks = 40, freq = FALSE,
     main = "Sampled angles", xlab = "W")
}

}
\seealso{
\code{\link{angular_mcmc}}
}
